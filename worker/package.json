{
  "name": "pdf-data-extractor-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Orchestratore batch/bulk polizze — discovery ricorsiva, coda Postgres, loop sequenziale verso l'API di estrazione",
  "scripts": {
    "scan": "tsx src/index.ts scan",
    "dry-run": "tsx src/index.ts dry-run",
    "work": "tsx src/index.ts work",
    "report": "tsx src/index.ts report",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.13.3",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.14",
    "typescript": "^5.6.0"
  }
}
